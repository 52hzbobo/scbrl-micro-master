server:
  port: 8088
  context-path: /

spring:
  application:
    name: scweb-service
  mvc:
    view:
      prefix: /templates/
      suffix: .ftl
  freemarker:
      cache: false
      request-context-attribute: request

# Eureka配置 发现注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://15959276686:bruceliu@127.0.0.1:8000/eureka/
